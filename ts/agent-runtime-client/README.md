# @gaoge/agent-runtime-client

Framework-neutral TypeScript client for Agent Runtime HTTP v1. Configure one
`RuntimeClient` with a base URL, fetch implementation and asynchronous headers,
then use its grouped `runs`, `events`, `interactions`, `outputs`, `evidence`,
`queue` and `workbench` APIs.
